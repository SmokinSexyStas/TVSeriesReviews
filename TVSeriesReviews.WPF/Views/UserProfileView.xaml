<UserControl x:Class="TVSeriesReviews.WPF.Views.UserProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TVSeriesReviews.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Background="#3D445E">
        <TextBlock Text="{Binding Login}" FontSize="16" TextAlignment="Center"
                   Foreground="#FFFFFF"
                   FontFamily="Inter"
                   Margin="0 20"/>
        <Button Content="Exit profile"
                Command="{Binding ExitProfileCommand}"
                Width="100"
                Cursor="Hand"
                Background="#5A627F"
                Foreground="#FFFFFF"
                FontFamily="Inter"
                FontSize="14"
                BorderThickness="0"/>
    </StackPanel>
</UserControl>
